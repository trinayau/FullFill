import {createTheme} from "@mui/material"

export const theme = createTheme({
    palette: {
        primary: {
            main: "#00bcd4",
            light: "skyblue"
        },
        secondary: {
            main: "#ff4081",
        },
        otherColour: {
            main: "#999999"
        }
    }
})
